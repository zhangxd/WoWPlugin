# 任务模块需求规格

- 文档类型：需求
- 状态：已完成
- 主题：quest
- 适用范围：`quest` 当前已实现的独立任务界面、任务浏览与 Quest Inspector 基线
- 关联模块：`quest`、`minimap_button`
- 关联文档：
  - `docs/features/quest-features.md`
  - `docs/designs/quest-design.md`
  - `docs/tests/quest-test.md`
  - `docs/specs/encounter-journal-spec.md`
- 最后更新：2026-04-16

## 1. 背景

- 当前代码已经把原先挂在冒险指南里的任务能力拆成独立 `quest` 模块，但仓库内还没有与之对应的需求基线文档。
- 需要一份按当前实现整理的规格文档，明确 `quest` 模块的边界、入口和验收口径。

## 2. 目标

- 明确 `quest` 当前已经交付的能力范围。
- 明确 `quest` 与 `encounter_journal` 的职责分界。
- 为后续继续扩展独立任务界面时提供稳定的历史对照面。

## 3. 范围

### 3.1 In Scope

- 独立任务界面 `ToolboxQuestFrame`。
- 底部两个视图页签：`active_log` 当前任务视图、`map_questline` 任务线浏览视图。
- 左上角通用导航路径与可点击祖先回退。
- `map_questline` 视图内部的资料片 / 地图 / 任务线节点导航。
- 搜索框过滤任务线与任务名称。
- 最近完成任务记录与展示。
- 任务 tooltip、任务详情弹框、弹框回跳。
- 点击任务后触发的聊天调试输出。
- `quest` 设置页中的 Quest Inspector 子页面。
- 小地图飞出菜单“任务”入口。

### 3.2 Out of Scope

- 冒险指南副本列表、详情页和锁定摘要增强。
- `quest_type` 视图。
- 冒险指南根页签顺序与显隐管理。
- 额外 slash 命令、额外菜单按钮或额外 TOC 行之外的隐藏入口。
- 任务数据导出链路本身的设计与实现。

## 4. 已确认决策

- 主归属模块为 `quest`。
- 主界面宿主为独立 `ToolboxQuestFrame`，不再依附 `EncounterJournal` 根框体。
- 主界面采用不透明底板，并提供独立标题栏；视觉层次参考冒险指南的“标题栏 + 主体内容区”结构。
- 底部固定保留两个视图页签：`active_log` 与 `map_questline`；默认打开逻辑保持现状，不额外改默认入口。
- `active_log` 视图不再复用左树 + 右区布局，而是改为上下两段：上方当前任务固定显示并可内部滚动，下方历史完成列表可折叠；折叠后上方区域占满。
- 每个视图都可以注册自己的导航节点；`active_log` 至少注册当前视图根节点，供左上角通用导航路径使用。
- `map_questline` 视图继续使用左侧导航 + 右侧主区，但左侧不再提供“当前任务”入口。
- `quest` 内部导航改为“通用节点 + 当前选中节点 + 导航路径”模型；节点只描述父子关系与上下文，不承载页面跳转回调。
- 左上角导航路径显示当前选中节点的祖先链，且祖先节点支持点击回退。
- `map_questline` 视图顶部路径导航改为接入 Blizzard `NavBar` 组件，视觉对齐冒险指南地下城页签的原生路径条，而不是继续使用自绘按钮条。
- 顶部路径导航锚到右侧内容区头部，左侧避让宿主头像 / 标题图标区，右侧避让搜索框，不再与宿主左上角图标重叠。
- 任务导航、任务列表、任务详情与 Quest Inspector 数据统一通过 `Toolbox.Questlines` 领域 API 获取。
- 最近完成任务通过 `QUEST_TURNED_IN` 事件维护，保存在 `ToolboxDB.modules.quest.questRecentCompletedList`。
- Quest Inspector 归属 `quest` 模块设置页，不再属于 `encounter_journal`。
- 小地图飞出菜单中的“任务”入口用于打开 `quest` 主界面；打不开时回退到 `quest` 设置页。
- 通用节点导航本轮仅落在 `quest` 模块内部，不上提到 `Core` 级通用层。

## 5. 待确认项

- 无。本文件只描述当前已实现内容，不新增待决设计选项。

## 6. 验收标准

1. 小地图飞出菜单中的“任务”入口可以打开独立 `quest` 主界面。
2. 设置页“任务”模块中可以点击按钮打开同一主界面。
3. 主界面底部固定显示 `当前任务` 与 `任务线` 两个视图页签，并可相互切换。
4. `当前任务` 视图不显示左树导航，而是显示上下两段内容：上方为当前任务列表，下方为历史完成列表。
5. 当前任务列表固定可见，每个任务一行；列表超出可滚动。
6. 历史完成列表条数受 `questRecentCompletedMax` 控制，可折叠；折叠后当前任务列表占满内容区。
7. `任务线` 视图继续提供左侧层级导航与右侧内容区，但左侧不再混入“当前任务”入口。
8. `任务线` 视图左上角显示通用导航路径，路径中的祖先节点可点击回退。
9. `任务线` 视图顶部路径导航使用 Blizzard `NavBar` 同类样式，末级节点表示当前位置，祖先节点可点击回退。
10. `任务线` 视图顶部路径导航不会被宿主左上角头像 / 图标盖住，且不会与右上角搜索框重叠。
11. `任务线` 视图左侧层级由 `quest` 内部注册的通用导航节点树驱动，点击节点后右侧按当前选中节点上下文刷新。
12. 点击任务线后会在主区展开对应任务列表，再次点击可折叠。
13. 搜索框可以按当前视图支持的任务线名或任务名过滤内容。
14. 点击任务后会显示详情弹框；若存在任务线归属，可回跳到对应地图 / 任务线。
15. 点击任务后会将当前可解析到的运行时详情分段输出到聊天框。
16. 在 Quest Inspector 子页面输入合法 `QuestID` 后，可看到可复制的结果文本；若任务缓存未就绪，页面会在异步加载完成后刷新。
17. 当前实现中不再暴露 `quest_type` 视图，也不再依赖 `encounter_journal` 作为任务浏览入口。

## 7. 实施状态

- 当前状态：已完成；`map_questline` 视图顶部路径导航的 `NavBar` 收口方案已确认，当前子任务可执行。
- 下一步：按已确认方案把顶部路径导航切到 Blizzard `NavBar`，并完成避让头像 / 搜索框的布局调整与回归验证。

## 8. 修订记录

| 日期 | 内容 |
|------|------|
| 2026-04-15 | 初稿：按当前 `quest` 模块实现建立独立任务界面与 Quest Inspector 的需求基线 |
| 2026-04-16 | 导航重构：确认底部双视图、左上角通用导航路径、`active_log` 上下布局与 `map_questline` 节点驱动左树 |
| 2026-04-16 | 确认 `map_questline` 顶部路径导航收口方案：改用 Blizzard `NavBar` 同类样式，并把锚点移到右侧内容区头部以避让宿主头像 / 搜索框 |
