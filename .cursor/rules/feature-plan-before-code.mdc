---
description: 新功能须先方案评估再改业务代码；与 AGENTS.md 新功能门禁一致
alwaysApply: true
---

# 新功能：先方案，再编码

本仓库 **WoWPlugin / Toolbox** 中，凡涉及以下任一类改动，**禁止**在首轮或未经用户确认前，对 `Toolbox/Core/**`、`Toolbox/Modules/**`、`Toolbox/UI/**`、`Toolbox/Toolbox.toc` 使用**写入类**工具（编辑、创建业务 Lua/TOC 等）：

- 新 **模块**（`RegisterModule`、新 `Modules/*.lua`）
- 新 **存档键**（`ToolboxDB.modules.*`、`ToolboxDB.global` 新字段）
- 新 **入口**（小地图按钮、游戏菜单、斜杠子命令、设置子页行为等）
- **`Toolbox.toc` 增删改加载行**

**必须先做**：用中文给出简短 **方案**（领域、数据落点、是否复用领域对外 API、验收标准）+ **1～3 个待确认问题**。建议对照 [docs/AI-ONBOARDING.md](docs/AI-ONBOARDING.md) §1.2 与根目录 [AGENTS.md](AGENTS.md)。

**仅在**用户明确回复 **「开动」** 或对分叉选项逐条选定后，方可修改上述业务路径。

例外：纯错别字、与行为无关的注释、不涉及上述四类改动的编辑，可不经「开动」。
